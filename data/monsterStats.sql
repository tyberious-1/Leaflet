create table monsterstats(
    monname text primary key,
    armorclass int,
    hitdice text,
    numofattacks text,
    damage text,
    movement text,
    numappearing text,
    saveas text,
    morale text,
    treasuretype text,
    xp text,
    descript text
);